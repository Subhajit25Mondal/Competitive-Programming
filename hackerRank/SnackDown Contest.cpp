#include<iostream>
#include<vector>
#include<bits/stdc++.h> 
using namespace std;
int main(int argc, char const *argv[])
{
    int t;
    vector<int>::iterator it; 
    cin>>t;
    for(int i = 0; i < t; i++)
    {
        int pr; cin>>pr;
        int p; cin>>p;
        vector <int> a;
        for(int j = 0; j < p; j++)
        {
            int temp; cin>>temp;
            a.push_back(temp);
        }
        int q; cin>>q;
        for(int j = 0; j < q; j++)
        {
            int temp; cin>>temp;
            a.push_back(temp);
        }

        for
        
        
    }
    
    return 0;
}
